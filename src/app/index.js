import React from "react";
import { render } from "react-dom";
import App from "./App";

render(<App />, document.getElementById("app")); //I put the code in the div that have the id 'app' in my index.html
